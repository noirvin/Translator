<!DOCTYPE html>
<html>
  <head>
    <title>Edit Entry</title>
    <link rel='stylesheet' href="https://bootswatch.com/4/superhero/bootstrap.min.css" />
  </head>
  <body>
    <div class="container">
      <h3><a href="/">Entry List</a></h3>
      <h1>Edit Entry</h1>
      <form action="/entries/update/<%= entry._id%>" method="post">
          <input type="hidden" name="entry_id" value="<%=entry._id%>">
        <table>
          <tbody>
            <tr>
              <td>Title</td>
              <td><input type="text" name="title" value="<%= entry.title %>" /></td>
            </tr>
            <tr>
              <td>Content</td>
              <td><textarea name="binaryCode"><%= entry.binaryCode %></textarea></td>
            </tr>
            <tr>
              <td>Content</td>
              <td><textarea name="nucleotideSequence"><%= entry.nucleotideSequence %></textarea></td>
            </tr>

            <tr>

              <td colspan="2"><button type="submit">Save Changes</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
  </body>
</html>
